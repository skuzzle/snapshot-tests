/**
 * JUnit5 extension implementation for snapshot tests. See the documentation for
 * {@link de.skuzzle.test.snapshots.junit5.EnableSnapshotTests} for a detailed explanation
 * of how to use this extension.
 * <p>
 * Starting with version 1.7.0, this package is no longer considered internal. Instead it
 * now represents the officially supported entry point for the snapshot test library when
 * using it from junit5.
 */
@API(status = Status.STABLE, since = "1.7.0")
package de.skuzzle.test.snapshots.junit5;

import org.apiguardian.api.API;
import org.apiguardian.api.API.Status;
